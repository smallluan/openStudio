/**
 * Placeholder / hook for optional OpenClaw-side notes applied at install time.
 * Intentionally a no-op so `npm ci`/`postinstall` succeeds when no memo is configured.
 */
console.log("[apply-openclaw-memo] no memo steps configured");
